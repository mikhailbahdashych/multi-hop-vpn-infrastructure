---
- name: restart wireguard
  ansible.builtin.shell: |
    for iface in {{ wg_interfaces | join(' ') }}; do
      wg-quick down "$iface" 2>/dev/null || true
      wg-quick up "$iface"
    done
  args:
    executable: /bin/bash
